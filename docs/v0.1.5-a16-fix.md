
# v0.1.5-a16 – testfix

Denne deltaen retter a16 uten å øke versjonen.

- gjenoppretter den etablerte `Arkade5Candidate`-API-en brukt av GUI/A11/A7,
- beholder `discover_arkade5_reports(report_path)` for eksisterende kode,
- legger automatisk workflow-søk i egen `discover_arkade5_reports_in_roots(...)`,
- hindrer at `external_evidence/arkade5/.../source` blir oppdaget på nytt,
- oppdaterer kontrakttestene til at `Noark 5 – standard` nå har 7 operasjoner,
- oppdaterer profiltesten til den nye `import_arkade5_reports`-operasjonen.

Versjonen forblir `0.1.5-a16`.
